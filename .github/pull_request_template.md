## What & why

<!-- What does this PR change, and why? Link any relevant task/issue. -->

## How I tested it

<!-- Briefly, steps, commands, or screenshots showing it works. -->

## Checklist

- [ ] Branched off an up-to-date `main`, PR targets `main`
- [ ] New dependencies (if any) added with `uv add`, with `pyproject.toml` and `uv.lock` both committed
- [ ] `uv sync` and the code run clean on a fresh checkout
- [ ] No data files or secrets committed
- [ ] Requested a review from a teammate
